plugins: ['svn']

vcs:
    url: svn://zon/projecten/zicht-libraries/ZichtVersioningBundle

tasks:
    ci.phpunit:
        pre: composer install --dev
        do: ./vendor/bin/phpunit --exclude-group=integration
